The .glb files in the 'tops' and 'legs' subdirectories are placeholders.
To make the 3D table configurator work correctly, replace these empty files
with actual .glb 3D models corresponding to the names (e.g., wood.glb, classic.glb).
The application expects these files to be valid GLTF 2.0 binary (.glb) format.
